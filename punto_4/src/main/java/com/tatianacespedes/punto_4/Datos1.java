/*package com.tatianacespedes.punto_4;

import android.app.DatePickerDialog;
import android.content.Context;
import android.widget.DatePicker;
import android.widget.Toast;

import java.util.Calendar;

public class Datos1 implements DatePickerDialog.OnDateSetListener {
    Context context;
    public Datos1(Context context){
        this.context= context;
    }

    @Override
    public void onDateSet(DatePicker view, int year, int monthOfYear, int dayOfMonth) {
        Toast.makeText(context,"Fecha selecionada: " +dayOfMonth+" / " + monthOfYear+ " / "+ year,Toast.LENGTH_LONG).show();
    }
}
*/